// all files are server components by default
// this means that we have access to nodejs-type functionality
// for instance....

// console.log(process)

// returns the {process} object

const SettingsPage = () => {
  return <div>Settings Page</div>
}

export default SettingsPage
